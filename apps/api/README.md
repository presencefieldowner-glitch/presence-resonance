# @presence-resonance/api

Backend API service exposing genealogy data, GEDCOM import/export, and resonance analysis to the web app and other clients.

Built with [Express](https://expressjs.com) and TypeScript. Exposes `GET /health` for now.

```bash
pnpm --filter @presence-resonance/api dev
```
